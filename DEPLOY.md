# راهنمای Deploy — Chativa (مرحله ۵)

این راهنما نصب کامل روی cPanel را از صفر توضیح می‌دهد، شامل بخش PHP (چت) و بخش اختیاری Node.js (Real-Time واقعی + تماس صوتی). اگر هاست شما «Setup Node.js App» ندارد، فقط بخش PHP را انجام دهید — همه‌چیز با polling هوشمند کار می‌کند و بعداً هر زمان Node در دسترس شد، بدون تغییر کد می‌توانید Real-Time را فعال کنید.

## بخش ۱ — نصب PHP (اجباری)

1. تمام فایل‌ها و پوشه‌ها **به‌جز** پوشه‌ی `node-server/` را داخل `public_html` (یا ساب‌فولدر دلخواه) آپلود کنید.
2. از **MySQL Databases** یک دیتابیس و کاربر با All Privileges بسازید.
3. آدرس سایت را باز کنید تا به `install.php` هدایت شوید؛ اطلاعات دیتابیس را وارد کنید.
4. یک حساب بسازید — **اولین کاربر ثبت‌نامی به‌صورت خودکار ادمین می‌شود.**
5. مجوزها: `uploads/` و `sessions/` باید قابل نوشتن بمانند (۷۵۵ کافی است). پوشه‌ی `sessions/` را خود پروژه می‌سازد و session کاربران را در آن ذخیره می‌کند تا مشکل رایج cPanel با `open_basedir` (که مسیر پیش‌فرض session سرور را غیرقابل‌دسترس می‌کند و باعث می‌شود لاگین بدون هیچ خطایی کار نکند) پیش نیاید؛ `config/`, `includes/`, `database/`, `logs/`, `sessions/` توسط `.htaccess` از دسترسی مستقیم مرورگر مسدود شده‌اند و نیازی به تغییر مجوز بیشتر ندارند.

تا همین‌جا سایت کاملاً کار می‌کند: چت عمومی، چت خصوصی، پیوست، Seen/Typing/بلاک، پنل ادمین — همه با polling هوشمند (بدون نیاز به Node).

## بخش ۲ — فعال‌سازی Real-Time واقعی با Node.js (اختیاری)

فقط اگر هاست شما گزینه‌ی **Setup Node.js App** (Node.js Selector) را در cPanel دارد.

### ۲.۱ آپلود و ساخت اپلیکیشن Node

1. پوشه‌ی `node-server/` را جداگانه (مثلاً در `~/chativa-realtime`، خارج از `public_html`) آپلود کنید. لازم نیست داخل `public_html` باشد.
2. در cPanel وارد **Setup Node.js App** شوید و «Create Application» را بزنید:
   - **Node.js version:** جدیدترین نسخه‌ی موجود (حداقل 16)
   - **Application mode:** Production
   - **Application root:** مسیر پوشه‌ای که `node-server/` را آپلود کردید (مثلاً `chativa-realtime`)
   - **Application URL:** یک ساب‌دامین یا مسیر اختصاصی برای Real-Time انتخاب کنید (مثلاً `ws.your-domain.com` یا `your-domain.com/realtime`). پیشنهاد می‌شود از یک **ساب‌دامین جدا** استفاده کنید تا SSL آن جداگانه و ساده مدیریت شود.
   - **Application startup file:** `server.js`
3. پس از ساخت اپ، در همان صفحه دکمه‌ی **«Run NPM Install»** را بزنید تا پکیج `ws` نصب شود.

### ۲.۲ تنظیم متغیرهای محیطی (Environment Variables)

در همان صفحه‌ی Node.js App، بخش Environment Variables را باز کنید و دو مقدار زیر را اضافه کنید:

| نام | مقدار |
|---|---|
| `WS_SECRET` | از پنل ادمین سایت PHP › تنظیمات › بخش Real-Time کپی کنید (به‌صورت خودکار تولید می‌شود) |
| `PHP_BASE_URL` | آدرس کامل سایت PHP، بدون `/` انتهایی، مثل `https://your-domain.com` |

بعد از افزودن، اپلیکیشن را **Restart** کنید تا متغیرها اعمال شوند.

### ۲.۳ فعال‌سازی از پنل ادمین PHP

1. به پنل ادمین سایت PHP بروید (`admin/settings.php`) و بخش «⚡ Real-Time (WebSocket)» را باز کنید.
2. تیک «فعال کردن Real-Time واقعی» را بزنید.
3. در «آدرس سرور Node» آدرس همان Application URL که در مرحله ۲.۱ ساختید را وارد کنید (مثلاً `https://ws.your-domain.com`) — با `https://`، بدون `/` انتهایی.
4. ذخیره کنید. مقدار `WS_SECRET` که در همین صفحه نمایش داده می‌شود باید دقیقاً همانی باشد که در مرحله ۲.۲ در Node کپی کردید (اگر آن را قبلاً کپی نکرده بودید، همین حالا کپی و در Node تنظیم کنید).
5. دکمه‌ی «تست اتصال به سرور Node» را بزنید — باید پیام موفقیت (health check سبز) نمایش داده شود.

از این لحظه، چت عمومی و خصوصی به‌صورت آنی (بدون تأخیر polling) کار می‌کنند و دکمه‌ی تماس صوتی زنده (📞) در چت خصوصی فعال می‌شود. اگر بعداً سرور Node خاموش یا غیرقابل‌دسترس شود، سایت به‌طور خودکار به polling برمی‌گردد — چیزی خراب نمی‌شود.

### ۲.۴ SSL و WSS

مرورگرها روی سایت‌های HTTPS اجازه‌ی اتصال WebSocket ناامن (`ws://`) نمی‌دهند. اگر سایت اصلی شما HTTPS است (که باید باشد)، آدرس Node هم باید HTTPS/WSS داشته باشد:

- اگر از ساب‌دامین استفاده کردید، از **AutoSSL** رایگان cPanel برای همان ساب‌دامین استفاده کنید (معمولاً خودکار صادر می‌شود).
- کد به‌صورت خودکار `https://` را به `wss://` تبدیل می‌کند؛ نیازی به تنظیم دستی نیست.

### ۲.۵ تماس صوتی زنده (WebRTC) و TURN

تماس صوتی از همان اتصال WebSocket برای سیگنالینگ استفاده می‌کند و نیازی به سرور اضافه ندارد. برای برقراری اتصال صدا به‌صورت پیش‌فرض از STUN عمومی گوگل استفاده می‌شود که برای اکثر شبکه‌ها کافی است.

اگر یکی از دو طرف پشت NAT محدودکننده/فایروال سخت‌گیر باشد (مثلاً شبکه‌ی شرکتی)، ممکن است تماس وصل نشود؛ در این صورت یک سرور TURN (مثل coturn روی یک VPS جدا — cPanel اشتراکی امکان اجرای آن را ندارد) راه‌اندازی کنید و اطلاعاتش را در همان بخش تنظیمات Real-Time (فیلدهای TURN URL / Username / Credential) وارد کنید. این بخش کاملاً اختیاری است.

## بخش ۳ — به‌روزرسانی از نسخه‌ی قبلی

کافی است فایل‌های جدید (شامل `node-server/` در صورت تمایل به Real-Time) را جایگزین/اضافه کنید. در اولین بازدید، سیستم مایگریشن خودکار ستون‌ها و جدول‌های جدید را می‌سازد — نیازی به اجرای دوباره‌ی `install.php` نیست.

> **نکته‌ی مرحله ۱۰:** اعلان مرورگر (Push Notification) از Notification API استاندارد استفاده می‌کند (نه Web Push سرور-محور)، پس نیازی به کلید/سرویس اضافه یا HTTPS اجباری ندارد. ارسال فایل/سند از همان مسیر آپلود عکس/ویدیو/ویس عبور می‌کند؛ اگر محدودیت `upload_max_filesize`/`post_max_size` را قبلاً برای ویدیو (۳۰ مگابایت) افزایش داده باشید، برای سندهای تا ۲۰ مگابایت هم کافی است.

## بخش ۴ — بررسی مشکلات رایج

| مشکل | راه‌حل |
|---|---|
| خطای 500 بعد از نصب | `config/config.php` › `APP_DEBUG = true` موقت، یا `logs/php-error.log` را بررسی کنید |
| لاگین کار نمی‌کند، ولی هیچ خطایی هم نشان نمی‌دهد (فرم فقط دوباره ظاهر می‌شود) | معمولاً یعنی session کاربر بین درخواست‌ها ذخیره نمی‌شود (اغلب به‌خاطر `open_basedir` که مسیر پیش‌فرض session سرور را مسدود کرده). پوشه‌ی `sessions/` باید وجود داشته و قابل‌نوشتن باشد (۷۵۵)؛ اگر با ۷۵۵ کار نکرد، ۷۷۵ را امتحان کنید. |
| آپلود عکس/ویدیو با خطا | در MultiPHP INI Editor مقدار `upload_max_filesize`/`post_max_size` را افزایش دهید |
| دکمه‌ی «تست اتصال» در تنظیمات قرمز است | مطمئن شوید Node App روشن است، `PHP_BASE_URL` و `WS_SECRET` درست تنظیم شده‌اند، و آدرس Real-Time دقیقاً با `https://` وارد شده |
| چت هنوز آنی نیست ولی Real-Time را فعال کردم | Console مرورگر را باز کنید؛ اگر خطای اتصال WebSocket دیدید، SSL ساب‌دامین Node را بررسی کنید (باید HTTPS معتبر داشته باشد) |
| تماس صوتی وصل نمی‌شود | Real-Time باید فعال و متصل باشد (بدون آن دکمه‌ی تماس کار نمی‌کند)؛ اگر همچنان مشکل بود، TURN را طبق بخش ۲.۵ تنظیم کنید |

## بخش ۵ — چک‌لیست نهایی قبل از انتقال به دامنه‌ی واقعی

- [ ] `install.php` را با اطلاعات دیتابیس واقعی اجرا کرده‌اید
- [ ] یک حساب ادمین ساخته‌اید (اولین کاربر ثبت‌نامی)
- [ ] مجوز پوشه‌ی `uploads/` قابل نوشتن است
- [ ] `APP_DEBUG` در `config/config.php` روی `false` است
- [ ] (اختیاری) Node.js App ساخته و روشن است، `WS_SECRET`/`PHP_BASE_URL` تنظیم شده
- [ ] (اختیاری) SSL روی ساب‌دامین Node فعال است و «تست اتصال» در پنل ادمین سبز است
- [ ] یک پیام آزمایشی در چت عمومی و یک گفتگوی خصوصی آزمایشی ارسال کرده‌اید
